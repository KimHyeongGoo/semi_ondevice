# Semi On-Device 플랫폼 기능 명세서

## 📋 플랫폼 개요

반도체 공정 장비(ALD/PVD)의 실시간 모니터링, 예측, 이상감지, 분석 및 제어를 위한 통합 플랫폼입니다. 
TensorFlow 기반 시계열 예측 모델과 XGBoost를 활용하여 공정 파라미터를 실시간으로 예측하고 이상 상황을 자동 감지하며, 웹 기반 대시보드를 통해 시각화 및 원격 제어를 제공합니다.

---

## 🎯 플랫폼 핵심 기능 분류

### 1. 실시간 데이터 수집 및 관리 (Real-time Data Ingestion & Management)

#### 1.1 다중 소스 데이터 수집
- **CSV 파일 기반 실시간 수집**
  - `realtimedata/YYYY/MM/DD/HH00.csv` 형식의 CSV 파일 자동 감시
  - Watchdog 기반 파일 시스템 이벤트 모니터링
  - 새로 생성되거나 수정된 파일을 실시간으로 감지
  - 파일별 오프셋 추적으로 중복 데이터 삽입 방지
  - 헤더 자동 인식 및 파싱
  - 인코딩 자동 처리 (UTF-8, UTF-8-sig)
  - NULL 문자 및 특수 문자 제거

- **과거 데이터 일괄 수집**
  - 디렉토리 구조 자동 탐색 (`YYYY/MM/DD/HH00.csv` 패턴)
  - 누락된 데이터만 선별하여 삽입
  - 중복 데이터 자동 필터링 (ON CONFLICT 처리)
  - 대용량 파일 배치 처리
  - 진행 상황 로깅

- **PVD 공정 데이터 수집**
  - PVD 전용 데이터 수집 파이프라인
  - Ion Gauge, Baratron Gauge, AR MFC 데이터 수집
  - 별도 테이블 구조로 관리 (`pvd4_new_*`)

#### 1.2 데이터베이스 관리
- **일별 테이블 자동 생성**
  - `rawdataYYYYMMDD` 형식의 일별 테이블 자동 생성
  - 테이블 존재 여부 확인 후 생성
  - 100개 이상의 컬럼 자동 스키마 정의
  - Primary Key: Timestamp
  - 데이터 타입 자동 매핑 (TIMESTAMP, REAL, INTEGER, TEXT, BOOLEAN)

- **데이터 무결성 보장**
  - Timestamp 기반 중복 방지
  - 데이터 타입 검증 및 변환
  - NULL 값 처리
  - 열거형 값 변환 (OPEN/CLOSE → 1/0)

- **데이터 보간 및 밀도화**
  - 1.2초 이상 간격이 있는 데이터 자동 보간
  - 1초 단위로 보간하여 시계열 연속성 확보
  - Forward fill 방식으로 누락값 채움
  - 시계열 데이터 정렬 및 정규화

- **데이터 마이그레이션 및 변환**
  - CSV 컬럼 구조 변경 지원
  - 타임스탬프 형식 변환
  - 이상 로그 데이터 마이그레이션
  - 스키마 버전 관리

#### 1.3 데이터 헬스체크 및 모니터링
- **생성기 상태 모니터링**
  - `generator_health.json` 파일 기반 헬스체크
  - 마지막 틱 시간 기록 및 조회
  - 3초 이내 업데이트 시 RUN 상태로 판정
  - 웹 API를 통한 실시간 상태 조회 (`/api/generator_status`)

- **프로세스 상태 추적**
  - 데이터 생성 프로세스 실행 상태 확인
  - 데이터 삽입 프로세스 실행 상태 확인
  - PID 기반 프로세스 추적
  - 프로세스 재가동 자동 감지

---

### 2. 실시간 시계열 예측 (Real-time Time Series Prediction)

#### 2.1 다중 파라미터 그룹 예측
- **5개 파라미터 그룹 병렬 예측**
  1. **MFC 가스 유량 그룹**: MFC7_DCS, MFC8_NH3, MFC26_F.PWR
  2. **MFC 질소 유량 그룹**: MFC1_N2-1, MFC2_N2-2, MFC3_N2-3
  3. **MFC 위치 및 전력 그룹**: MFC4_N2-4, MFC27_L.POS, MFC28_R.POS
  4. **압력 게이지 그룹**: VG11 Press value, VG12 Press value, VG13 Press value
  5. **온도 센서 그룹**: Temp_Act_U, Temp_Act_CU, Temp_Act_C, Temp_Act_CL, Temp_Act_L

- **다중 시간 스텝 예측**
  - 10초 후 값 예측
  - 20초 후 값 예측
  - 30초 후 값 예측
  - 각 스텝별 독립적인 예측 수행

#### 2.2 고급 모델 아키텍처
- **PatchTST (Patch-based Time Series Transformer)**
  - Patch Embedding 레이어: 시계열을 패치 단위로 변환
  - Positional Encoding 레이어: 시간적 위치 정보 인코딩
  - Transformer 기반 시퀀스 학습
  - 192초 윈도우 크기 (약 3분간의 시계열 데이터)

- **다중 모델 전략**
  - **일반 모델**: 전체 공정 단계에 적용
  - **보조 모델 (Main Process Model)**: 특정 공정 단계(111, 128, 119, 117, 152, 113, 115, 116)에 최적화
  - **온도 통합 모델**: 5개 온도 센서를 단일 모델로 일괄 예측
  - 공정 단계별 자동 모델 선택

- **특수 처리 기능**
  - VG11 Press value: Weighted MAE Loss 적용 (중심 구간 가중치 100배)
  - 온도 파라미터: 단일 모델로 5개 위치 동시 예측
  - 데이터 스케일링: 파라미터별 독립적인 MinMaxScaler 적용

#### 2.3 병렬 처리 및 성능 최적화
- **Ray 기반 분산 처리**
  - 파라미터 그룹별 독립적인 Ray 액터 생성
  - CPU/GPU 자동 선택
  - 비동기 예측 수행
  - 연결 풀 관리 (SimpleConnectionPool)

- **배치 예측 최적화**
  - 슬라이딩 윈도우 방식으로 배치 예측
  - 메모리 효율적인 데이터 처리
  - 예측 결과 버퍼링

- **데이터 전처리 최적화**
  - 시계열 데이터 보간
  - 결측값 처리 (Forward fill)
  - 데이터 정규화 및 스케일링

#### 2.4 예측 결과 저장 및 관리
- **테이블별 예측값 저장**
  - `pred_proc0` ~ `pred_proc4`: 파라미터 그룹별 예측 테이블
  - Timestamp, PredictStep, 예측값, 공정 단계 정보 저장
  - 중복 방지 (ON CONFLICT 처리)

- **예측 히스토리 관리**
  - 최대 48시간 데이터 보관 (설정 가능)
  - 오래된 데이터 자동 삭제
  - 예측 정확도 추적

---

### 3. 이상감지 및 알람 (Anomaly Detection & Alerting)

#### 3.1 예측 기반 이상감지
- **실제값 vs 예측값 비교**
  - 실시간으로 수집된 실제값과 예측값 비교
  - 상대 오차율 계산: `|실제값 - 예측값| / |실제값| * 100`
  - 절대 오차 계산: `|실제값 - 예측값|`

- **이상 판정 기준**
  - **상대 오차 임계값**: 10% 이상
  - **절대 오차 임계값**: 0.4 이상
  - 두 조건을 모두 만족해야 이상으로 판정
  - 최소 지속시간: 5초 이상
  - 정상 구간 2초 이상이면 이상 구간 종료

- **이상 구간 추적**
  - 이상 시작 시간 기록
  - 이상 종료 시간 기록
  - 지속시간 계산
  - 평균 오차율 계산
  - 최대 오차율 및 피크 시간 기록
  - 공정 단계 정보 수집

#### 3.2 한계값 기반 이상감지
- **공정 단계별 한계값 설정**
  - `limits.yaml` 파일 기반 설정
  - 파라미터별 상한선/하한선 정의
  - 공정 단계별 다른 한계값 적용
  - 전체 공정에 적용되는 기본 한계값 지원

- **인터락 한계값 설정**
  - `limits2.yaml` 파일 기반 설정
  - 인터락 트리거 조건 정의
  - 최소 지속시간: 3초 (limits.yaml), 1초 (limits2.yaml)

- **한계값 위반 감지**
  - 예측값이 상한선 초과 시 감지
  - 예측값이 하한선 미만 시 감지
  - 위반 유형 분류 (상한선/하한선)
  - 위반 지속시간 추적

#### 3.3 PVD 공정 이상감지
- **통계적 이상감지**
  - Ion Gauge: 10% 상대 오차 또는 절대값 기준
  - Baratron Gauge: 10% 상대 오차 또는 절대값 기준
  - AR MFC: 1% 상대 오차 기준
  - 평균값(mu) 기준 편차 계산

- **이상 데이터 저장**
  - `pvd4_abnormals` 테이블: 이상 데이터 상세 정보
  - `pvd_violence` 테이블: 이상 이벤트 로그
  - 소스 테이블 정보 추적
  - Set ID 및 상태 정보 관리

#### 3.4 이상 이벤트 관리
- **이상 로그 저장**
  - `realtime_abnormal_log`: 예측 기반 이상감지 로그
  - `realtime_abnormal_log2`: 한계값 기반 이상감지 로그
  - 시작/종료 시간, 지속시간, 오차율, 피크 정보 저장
  - 공정 단계 정보 포함
  - Violation Type 분류 (1-4 등급)

- **이상 이벤트 우선순위**
  - 기존 이상 구간과의 겹침 확인
  - 겹치는 구간의 Violation Type 상속
  - 새로운 이상 구간에 대한 가중치 기반 Type 할당
  - Type 1: 40%, Type 2: 30%, Type 3: 20%, Type 4: 10%

- **이상 이벤트 업데이트**
  - 실시간으로 이상 구간 확장/축소 추적
  - 종료 시간 자동 업데이트
  - 평균/최대 오차율 재계산
  - ON CONFLICT 기반 업데이트

#### 3.5 알람 및 알림 시스템
- **텔레그램 알림**
  - 이상 상황 발생 시 자동 알림
  - 장비 상태 변경 알림
  - 커스텀 메시지 전송
  - HTML 형식 메시지 지원
  - 환경 변수 기반 설정 (토큰, 채팅 ID)

- **웹 기반 알람 표시**
  - 실시간 이상감지 로그 표시
  - 알람 이력 조회
  - 기간별 알람 필터링
  - 파라미터별 알람 필터링

- **장비 시작 그레이스 기간**
  - 장비 시작 후 60초 동안 이상감지 비활성화
  - `generator_health.json`에서 장비 시작 시간 읽기
  - 프로세스 재가동 감지 및 그레이스 기간 적용

---

### 4. 공정 모니터링 및 분석 (Process Monitoring & Analysis)

#### 4.1 공정 구간 감지
- **공정 시작/종료 감지**
  - START 단계 감지로 공정 시작 판정
  - END, STANDBY, IDLE 등으로 공정 종료 판정
  - 공정 구간 자동 추출
  - 다중 공정 구간 처리

- **공정 단계 추적**
  - 현재 실행 중인 공정 단계 조회
  - 공정 단계별 파라미터 모니터링
  - 공정 단계 전환 감지
  - 공정 단계별 통계 수집

#### 4.2 박막 두께 예측
- **XGBoost 기반 두께 예측**
  - 45개 포인트 예측 (9개 포인터 × 5개 웨이퍼 위치)
  - 공정 파라미터 기반 예측
  - 웨이퍼 위치별 두께 분포 예측
  - GPU/CPU 자동 선택

- **두께 데이터 저장**
  - `trace_info` 테이블에 두께 데이터 저장
  - 공정 시작/종료 시간과 함께 저장
  - 히트맵 생성용 데이터 제공

- **히트맵 시각화**
  - Inverse Distance Weighting (IDW) 보간
  - 9개 포인터 위치 기반 보간
  - 웨이퍼별 히트맵 이미지 생성
  - PNG 형식으로 저장 및 웹 표시

#### 4.3 공정 파라미터 예측
- **STANDARD 모델 기반 예측**
  - 공정 구간의 파라미터 예측
  - `trace_parameter_pred` 테이블에 저장
  - 공정 품질 평가용 데이터 제공

#### 4.4 공정 이력 관리
- **장비 이력 저장**
  - `equipment_history` 테이블에 공정 이력 저장
  - PJOB_ID, ProcessRecipe, 시작/종료 시간 저장
  - 종료 상태(EndStatus) 기록
  - 레시피별 이력 조회

- **CSV 파일 연동**
  - 공정별 CSV 파일 목록 조회
  - CSV 파일 데이터 조회
  - 파일 수정 시간 추적
  - 레시피 이름 매핑

#### 4.5 공정 비교 및 분석
- **공정 간 비교 기능**
  - 여러 공정의 파라미터 비교
  - 차트 기반 시각적 비교
  - 통계적 비교 분석

---

### 5. 실시간 시각화 및 대시보드 (Real-time Visualization & Dashboard)

#### 5.1 실시간 모니터링 대시보드
- **메인 대시보드 (`/`)**
  - 실시간 파라미터 모니터링
  - 실제값 vs 예측값 동시 표시
  - 다중 파라미터 동시 모니터링
  - 실시간 차트 업데이트 (폴링 기반)
  - 한계값 라인 표시
  - 공정 단계 정보 표시

- **실시간 이상감지 화면 (`/index2.html`, `/index4.html`)**
  - 이상감지 로그 실시간 표시
  - 이상 구간 시각화
  - 파라미터별 필터링
  - 시간대별 필터링
  - Violation Type 표시
  - 상세 정보 팝업

- **공정 추적 화면 (`/index3.html`)**
  - 공정 정보 및 두께 데이터 표시
  - 히트맵 시각화
  - 공정 이력 조회
  - 공정 비교 기능

- **PVD 모니터링 화면 (`/pvd`)**
  - PVD 실시간 스트림 데이터 표시
  - Ion Gauge, Baratron Gauge, AR MFC 모니터링
  - 이상 데이터 하이라이트
  - 실시간 차트 업데이트

#### 5.2 차트 및 그래프
- **시계열 차트**
  - Chart.js 기반 시계열 차트
  - 실제값/예측값 동시 표시
  - 줌 및 팬 기능
  - 시간 범위 선택
  - 다중 Y축 지원

- **이벤트 차트**
  - 특정 시간대의 파라미터 변화 표시
  - 이상 구간 하이라이트
  - 공정 단계 전환 표시
  - 마커 및 주석 기능

- **히트맵 차트**
  - 웨이퍼 두께 분포 히트맵
  - 9개 포인터 위치 표시
  - 컬러맵 기반 시각화
  - 웨이퍼별 히트맵 비교

- **공정 비교 차트**
  - 여러 공정의 파라미터 비교
  - 오버레이 차트
  - 통계 정보 표시

#### 5.3 데이터 조회 및 필터링
- **시간 범위 조회**
  - 최근 N초간 데이터 조회 (기본 300초)
  - 시작/종료 시간 지정 조회
  - 날짜별 데이터 조회

- **파라미터 필터링**
  - 파라미터별 데이터 조회
  - 다중 파라미터 동시 조회
  - 파라미터 그룹별 조회

- **공정 단계 필터링**
  - 특정 공정 단계 데이터 조회
  - 공정 단계별 통계
  - 공정 단계 전환 이벤트 조회

#### 5.4 로그 뷰어
- **로그 차트 뷰어 (`/logview.html`)**
  - 이상감지 로그 시각화
  - 시간대별 로그 필터링
  - 상세 로그 정보 표시
  - 로그 메시지 파싱 및 표시

---

### 6. 장비 제어 및 관리 (Equipment Control & Management)

#### 6.1 원격 장비 제어
- **장비 시작/중지**
  - 웹 API를 통한 장비 제어
  - `generate_data.py` 프로세스 시작/중지
  - `insert_real_time.py` 프로세스 시작/중지
  - 프로세스 상태 실시간 확인
  - 다중 프로세스 동시 제어

- **프로세스 관리**
  - 프로세스 실행 상태 조회
  - 프로세스 PID 추적
  - 프로세스 그룹 관리
  - 자식 프로세스까지 종료
  - SIGTERM/SIGKILL 신호 전송

- **텔레그램 기반 원격 제어**
  - 텔레그램 봇을 통한 장비 제어
  - 인라인 키보드 버튼 제공
  - 장비 DOWN 버튼 클릭 시 즉시 중지
  - 취소 버튼으로 작업 취소
  - 결과 메시지 자동 전송

#### 6.2 장비 상태 모니터링
- **실시간 상태 조회**
  - 장비 실행/중지 상태 확인
  - 데이터 생성기 상태 확인
  - 데이터 삽입 프로세스 상태 확인
  - 헬스체크 정보 조회

- **프로세스 재가동 감지**
  - 프로세스 PID 변경 감지
  - 프로세스 재시작 자동 감지
  - 그레이스 기간 자동 적용
  - 상태 변경 로깅

#### 6.3 설정 관리
- **한계값 설정**
  - 웹 인터페이스를 통한 한계값 설정
  - 파라미터별 상한선/하한선 설정
  - 공정 단계별 다른 한계값 설정
  - YAML 파일 기반 저장
  - 실시간 적용

- **시스템 설정**
  - `settings.yaml` 파일 기반 설정
  - 웹 인터페이스를 통한 설정 변경
  - 설정 조회 및 저장 API

- **인터락 설정**
  - `limits2.yaml` 파일 기반 인터락 설정
  - 인터락 트리거 조건 설정
  - 최소 지속시간 설정

---

### 7. 데이터 분석 및 리포트 (Data Analysis & Reporting)

#### 7.1 배치 데이터 분석
- **과거 데이터 재분석**
  - 특정 기간의 데이터 일괄 처리
  - 예측값 재계산
  - 이상감지 재수행
  - 결과 재저장

- **CSV 파일 기반 분석**
  - `abnormal_data/` 디렉토리의 CSV 파일 처리
  - 파일별 독립적 처리
  - 진행 상황 로깅
  - 결과 통계 수집

- **데이터베이스 기반 분석**
  - `rawdataYYYYMMDD` 테이블에서 데이터 조회
  - 일별 테이블 단위 처리
  - 시간 범위 지정 처리
  - 대용량 데이터 효율적 처리

#### 7.2 통계 분석
- **파라미터 통계**
  - 평균값, 최대값, 최소값 계산
  - 표준편차 계산
  - 분포 분석
  - 공정 단계별 통계

- **이상감지 통계**
  - 이상 발생 빈도 분석
  - 이상 지속시간 통계
  - 파라미터별 이상 발생률
  - 공정 단계별 이상 발생률
  - Violation Type별 분포

- **예측 정확도 분석**
  - 실제값 vs 예측값 오차 분석
  - 평균 절대 오차 (MAE) 계산
  - 평균 상대 오차 계산
  - 시간대별 정확도 추적

#### 7.3 리포트 생성
- **알람 이력 리포트**
  - 기간별 알람 발생 현황
  - 파라미터별 알람 통계
  - 공정 레시피별 알람 통계
  - Violation Type별 분류

- **공정 이력 리포트**
  - 공정 실행 이력
  - 공정 성공/실패 통계
  - 레시피별 실행 통계
  - 시간대별 실행 통계

- **CSV 데이터 리포트**
  - CSV 파일별 데이터 요약
  - 파일 수정 시간 추적
  - 레시피 매핑 정보

---

### 8. 통합 및 연동 (Integration & Connectivity)

#### 8.1 REST API 제공
- **데이터 조회 API**
  - `/api/data`: 최근 데이터 조회
  - `/api/trace_info`: 공정 정보 조회
  - `/api/current_step`: 현재 공정 단계 조회
  - `/api/process_range`: 공정 시간 범위 조회
  - `/api/equipment_history`: 장비 이력 조회
  - `/api/csv_files`: CSV 파일 목록 조회
  - `/api/csv_data`: CSV 파일 데이터 조회

- **이상감지 API**
  - `/api/logs`: 실시간 이상감지 로그
  - `/api/anomaly_logs`: 이상감지 로그 (별칭)
  - `/api/prediction_logs`: 예측 기반 이상감지 로그
  - `/api/history/logs`: 기간별 로그 조회
  - `/api/alarm_history`: 알람 이력 조회

- **차트 데이터 API**
  - `/api/event_chart`: 이벤트 차트 데이터
  - `/api/trace_pred_chart`: 공정 예측 차트 데이터

- **설정 관리 API**
  - `/api/limits`: 한계값 설정 조회/저장
  - `/api/interlock_limits`: 인터락 한계값 설정
  - `/api/settings`: 시스템 설정 조회/저장
  - `/api/model_columns`: 모델 컬럼 목록 조회

- **장비 제어 API**
  - `/api/equipment/start`: 장비 시작
  - `/api/equipment/stop`: 장비 중지
  - `/api/equipment/status`: 장비 상태 조회
  - `/api/generator_status`: 생성기 상태 조회

- **텔레그램 API**
  - `/api/telegram/notify`: 텔레그램 알림 전송
  - `/api/telegram/webhook`: 텔레그램 봇 웹훅

#### 8.2 텔레그램 봇 연동
- **알림 기능**
  - 이상 상황 발생 시 자동 알림
  - 장비 상태 변경 알림
  - 커스텀 메시지 전송
  - HTML 형식 메시지 지원

- **원격 제어 기능**
  - 인라인 키보드 버튼 제공
  - 장비 시작/중지 버튼
  - 취소 버튼
  - 실시간 상태 피드백

- **웹훅 처리**
  - Callback query 처리
  - 버튼 클릭 이벤트 처리
  - 결과 메시지 자동 전송

#### 8.3 데이터베이스 연동
- **PostgreSQL 통합**
  - 연결 풀 관리 (SimpleConnectionPool)
  - 트랜잭션 관리
  - 에러 핸들링 및 롤백
  - 비동기 쿼리 지원

- **테이블 자동 관리**
  - 일별 테이블 자동 생성
  - 스키마 버전 관리
  - 인덱스 최적화
  - 데이터 보관 정책

#### 8.4 파일 시스템 연동
- **CSV 파일 처리**
  - 파일 시스템 이벤트 모니터링
  - 파일 읽기/쓰기
  - 인코딩 자동 감지
  - 대용량 파일 처리

- **설정 파일 관리**
  - YAML 파일 읽기/쓰기
  - JSON 파일 읽기/쓰기
  - 파일 변경 감지
  - 설정 검증

---

### 9. 성능 최적화 및 확장성 (Performance & Scalability)

#### 9.1 병렬 처리
- **Ray 기반 분산 처리**
  - 파라미터 그룹별 독립적인 액터
  - CPU/GPU 자동 선택
  - 비동기 예측 수행
  - 리소스 관리

- **배치 처리 최적화**
  - 슬라이딩 윈도우 방식
  - 메모리 효율적인 데이터 처리
  - 배치 크기 자동 조정

#### 9.2 데이터베이스 최적화
- **연결 풀 관리**
  - SimpleConnectionPool 사용
  - 연결 수 제한 및 재사용
  - 연결 타임아웃 관리
  - 에러 복구

- **쿼리 최적화**
  - 인덱스 활용
  - 날짜 범위 기반 쿼리
  - 배치 삽입 (executemany)
  - 비동기 쿼리

#### 9.3 메모리 관리
- **데이터 스트리밍**
  - 대용량 데이터 스트리밍 처리
  - 청크 단위 처리
  - 메모리 사용량 모니터링

- **캐시 관리**
  - 모델 및 스케일러 캐싱
  - 설정 파일 캐싱
  - 결과 데이터 캐싱

#### 9.4 확장성
- **수평 확장 지원**
  - 분산 처리 아키텍처
  - 독립적인 서비스 구성
  - 로드 밸런싱 지원

- **모듈화된 구조**
  - 기능별 독립적인 모듈
  - 플러그인 방식 지원
  - 설정 기반 기능 활성화/비활성화

---

### 10. 보안 및 안정성 (Security & Reliability)

#### 10.1 에러 핸들링
- **예외 처리**
  - 모든 주요 함수에 try-except 블록
  - 에러 로깅
  - 에러 복구 메커니즘
  - 사용자 친화적 에러 메시지

- **데이터 검증**
  - 입력 데이터 검증
  - 타입 검증
  - 범위 검증
  - NULL 값 처리

#### 10.2 로깅 시스템
- **파일 기반 로깅**
  - `log/` 디렉토리에 로그 저장
  - 프로세스별 로그 파일
  - 타임스탬프 포함
  - 로그 레벨 관리

- **콘솔 로깅**
  - 실시간 로그 출력
  - 포맷팅된 로그 메시지
  - 진행 상황 표시

#### 10.3 데이터 백업
- **자동 백업**
  - 중요 데이터 자동 백업
  - 백업 파일 관리
  - 복구 메커니즘

#### 10.4 프로세스 관리
- **그레이스풀 셧다운**
  - SIGTERM/SIGINT 신호 처리
  - 연결 풀 정리
  - 리소스 해제
  - 안전한 종료

---

## 📊 플랫폼 데이터 흐름도

```
[장비 데이터] 
    ↓
[CSV 파일 생성] (generate_data.py)
    ↓
[파일 시스템 감시] (insert_real_time.py)
    ↓
[PostgreSQL 저장] (rawdataYYYYMMDD)
    ↓
    ├─→ [실시간 예측] (predict_real_time.py) → [예측값 저장] (pred_proc{idx})
    │       ↓
    │   [이상감지] (abnormal_monitor2.py) → [이상 로그] (realtime_abnormal_log)
    │
    ├─→ [공정 감지] (insert_trace_info.py) → [공정 정보] (trace_info)
    │       ↓
    │   [두께 예측] (XGBoost) → [두께 데이터] (trace_info)
    │
    └─→ [웹 대시보드] (FastAPI) → [사용자 인터페이스]
            ↓
        [텔레그램 알림] (텔레그램 봇)
```

---

## 🔧 기술 스택

### 백엔드
- **Python 3.x**
- **FastAPI**: 웹 프레임워크
- **PostgreSQL**: 데이터베이스
- **TensorFlow/Keras**: 딥러닝 모델
- **XGBoost**: 두께 예측 모델
- **Ray**: 분산 처리
- **Pandas**: 데이터 처리
- **NumPy**: 수치 계산
- **Joblib**: 모델 저장/로드

### 프론트엔드
- **HTML/CSS/JavaScript**
- **Chart.js**: 차트 라이브러리
- **Three.js**: 3D 시각화 (PVD)
- **Jinja2**: 템플릿 엔진

### 인프라
- **Watchdog**: 파일 시스템 모니터링
- **Psycopg2**: PostgreSQL 드라이버
- **Matplotlib**: 히트맵 생성
- **Requests**: HTTP 클라이언트

---

## 📈 모니터링 파라미터 목록

### MFC (Mass Flow Controller) 파라미터
- MFC1_N2-1, MFC2_N2-2, MFC3_N2-3, MFC4_N2-4: 질소 유량
- MFC7_DCS: Dichlorosilane 유량
- MFC8_NH3: 암모니아 유량
- MFC9_F2: 불소 유량
- MFC26_F.PWR: 전력
- MFC27_L.POS, MFC28_R.POS: 위치

### 압력 게이지 (Baratron Gauge) 파라미터
- VG11 Press value: 프로세스 중 압력
- VG12 Press value: 프로세스 외 압력 (1)
- VG13 Press value: 프로세스 외 압력 (2)

### 온도 센서 파라미터
- Temp_Act_U: 상부 온도
- Temp_Act_CU: 중앙 상부 온도
- Temp_Act_C: 중앙 온도
- Temp_Act_CL: 중앙 하부 온도
- Temp_Act_L: 하부 온도

### 밸브 상태 파라미터
- ValveAct_2:2 ~ ValveAct_90:PST: 다양한 밸브 상태

### PVD 파라미터
- ion_gauge_i: Ion Gauge 전류
- baratron_gauge_i: Baratron Gauge 전류
- ar_mfc_i: AR MFC 전류

---

## 🎯 주요 사용 사례

1. **실시간 공정 모니터링**: 공정 파라미터를 실시간으로 모니터링하고 이상 상황을 즉시 감지
2. **예측 기반 예방 정비**: 미래 값을 예측하여 문제 발생 전 대응
3. **공정 품질 분석**: 공정 이력 및 두께 데이터를 분석하여 품질 개선
4. **원격 모니터링 및 제어**: 웹 및 텔레그램을 통한 원격 모니터링 및 제어
5. **과거 데이터 분석**: 배치 처리를 통한 과거 데이터 재분석 및 패턴 발견
6. **알람 및 알림**: 이상 상황 발생 시 자동 알림 및 대응 가이드 제공

---

*작성일: 2025년*
*프로젝트: Semi On-Device Platform*
*버전: 1.0*

